@model dermai.Models.PielFormModel

<form asp-action="GuardarRutinaModificada" asp-controller="Home" method="post">

<section class="hacerRutina">
  <div class="características">
    <h2>¿Qué características tenés?</h2>
    <h3>(Podés elegir varias opciones)</h3>
    <div>
      @foreach (var item in new[] {
        "Piel sensible", "Enrojecimiento frecuente", "Tendencia al acné", "Sequedad / descamación",
        "Opacidad / falta de luminosidad", "Poros dilatados", "Puntos negros",
        "Manchas oscuras / hiperpigmentación", "Textura irregular", "Exceso de grasa / brillo",
        "Líneas de expresión / arrugas"
      }) {
        <label>
          <input type="checkbox" name="Caracteristicas" value="@item" /> @item
        </label>
      }
    </div>
  </div>

  <div class="preferencias">
    <h2>¿Qué preferencias de productos tenés?</h2>
    <h3>(Podés elegir varias opciones)</h3>
    <div>
      @foreach (var item in new[] {
        "Productos sin fragancia", "Sin alcohol", "Silicona",
        "Ingredientes naturales / orgánicos", "Sin parabenos", "Cruelty-Free", "Vegano"
      }) {
        <label>
          <input type="checkbox" name="Preferencias" value="@item" /> @item
        </label>
      }
    </div>
  </div>

  <div class="presupuesto">
    <h2>¿Cuál es tu presupuesto estimado?</h2>
    <div>
      <label><input type="radio" asp-for="Presupuesto" value="Económico" /> Económico (hasta $50 USD)</label>
      <label><input type="radio" asp-for="Presupuesto" value="Medio" /> Medio (hasta $120 USD)</label>
      <label><input type="radio" asp-for="Presupuesto" value="Premium" /> Premium (desde $120 USD)</label>
    </div>
  </div>

  <div class="frecuenciaRutinaria">
    <h2>¿Cuál es tu frecuencia de rutina ideal?</h2>
    <div>
      <label><input type="radio" asp-for="Frecuencia" value="Simple" /> Rutina simple (1-3 pasos)</label>
      <label><input type="radio" asp-for="Frecuencia" value="Intermedia" /> Rutina intermedia (4-5 pasos)</label>
      <label><input type="radio" asp-for="Frecuencia" value="Completa" /> Rutina completa (6+ pasos)</label>
    </div>
  </div>
  
  <input type="submit" value="Enviar" class="btn btn-primary mt-3">
</section>
</form>